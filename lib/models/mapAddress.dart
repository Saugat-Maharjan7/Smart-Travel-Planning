class Address{
  String startAddress;
  String placeName;
  String placeId;
  double latitude;
  double longitude;

  Address({this.startAddress, this.placeName, this.placeId, this.longitude, this.latitude});
}